{{- $validCoordServices := list "" "CommonLoadBalancer" "LoadBalancer" "NodePort" }}
{{- if not (has .Values.externalAccessConfig.coordinator.serviceType $validCoordServices) }}
  {{- fail "Invalid value for externalAccessConfig.coordinator.serviceType. Use '', 'CommonLoadBalancer', 'LoadBalancer' or 'NodePort'. To expose coordinators via ingress-nginx, set externalAccessConfig.ingress.enabled: true instead." }}
{{- end }}

{{- if eq $.Values.externalAccessConfig.coordinator.serviceType "" }}
  {{- /* No external service rendered for empty string */}}
{{- else if eq $.Values.externalAccessConfig.coordinator.serviceType "CommonLoadBalancer" }}
apiVersion: v1
kind: Service
metadata:
  name: coordinators
  {{- with $.Values.externalAccessConfig.coordinator.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- with $.Values.labels.coordinators.serviceLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  type: LoadBalancer
  selector:
    role: coordinator
  ports:
    - protocol: TCP
      name: tcp-bolt-port
      port: {{ $.Values.ports.boltPort }}
      targetPort: {{ $.Values.ports.boltPort }}
{{- else }}
  {{- range .Values.coordinators }}
---
apiVersion: v1
kind: Service
metadata:
  name: memgraph-coordinator-{{ .id }}-external
  {{- $merged := merge (default dict .externalAccessAnnotations) (default dict $.Values.externalAccessConfig.coordinator.annotations) }}
  {{- with $merged }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
  labels:
    {{- with $.Values.labels.coordinators.serviceLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  {{- if eq $.Values.externalAccessConfig.coordinator.serviceType "LoadBalancer" }}
  type: LoadBalancer
  {{- else if eq $.Values.externalAccessConfig.coordinator.serviceType "NodePort" }}
  type: NodePort
  {{- end }}
  selector:
    app: memgraph-coordinator-{{ .id }}
  ports:
    - protocol: TCP
      name: tcp-bolt-port
      port: {{ $.Values.ports.boltPort }}
      targetPort: {{ $.Values.ports.boltPort }}
  {{- end }}
{{- end }}
