# Helm Chart for Memgraph's MCP server

The source code of Memgraph's MCP server is [here](https://github.com/memgraph/ai-toolkit/tree/main/integrations/mcp-memgraph). Before installing the Memgraph MCP server, make sure to check
`env` section in `values.yaml` and `authSecret` section. After that, you should be able to install your chart using:
